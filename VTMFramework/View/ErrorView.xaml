<UserControl x:Class="VtmFramework.View.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VtmFramework.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="579.872" Background="Green" VerticalAlignment="Center">
    <UserControl.DataContext>
        <vm:ErrorViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>

        <Image Grid.Row="1" Grid.Column="1" Width="32" Height="32" />
        <Label Grid.Row="1" Grid.Column="2" Content="{Binding Title}" />

        <Label Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="3" Content="{Binding Message}" />
        <ListView Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="3" Background="Transparent">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </ListView.Resources>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <Button Content="Hallo"></Button>
            <Button Content="Hallo"></Button>
            <Button Content="Hallo"></Button>
        </ListView>
    </Grid>
</UserControl>
