<UserControl x:Class="VtmFramework.View.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VtmFramework.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="292" d:DesignWidth="579.872" VerticalAlignment="Center" HorizontalAlignment="Center"
             Visibility="{Binding Visible, FallbackValue=Collapsed}" BorderThickness="2">
    <UserControl.DataContext>
        <vm:ErrorViewModel />
    </UserControl.DataContext>
    <UserControl.Background>
        <SolidColorBrush Color="White" Opacity="0.75"/>
    </UserControl.Background>
    <UserControl.BorderBrush>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="Red" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.BorderBrush>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>

        <Image Grid.Row="1" Grid.Column="1" Width="32" Height="32" Source="D:\Franzi Wimmer\Downloads\warning_shield_grey.png" />
        <Label Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="3" Content="{Binding Title, FallbackValue=Titel}" FontWeight="Bold" BorderThickness="0,0,0,1" BorderBrush="Black" VerticalContentAlignment="Center" Margin="1,0,19,0" />
        <Label Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Content="{Binding Message, FallbackValue=Nachricht}" />

        <StackPanel Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Background="{x:Null}" BorderBrush="Lime" BorderThickness="2,0,0,2" Margin="5,5,5,5" Padding="10,0,10,0" Command="{Binding ButtonYesCommand}">Ja</Button>
            <Button Background="{x:Null}" BorderBrush="Red" BorderThickness="2,0,0,2" Margin="5,5,5,5" Padding="10,0,10,0" Command="{Binding ButtonNoCommand}">Nein</Button>
            <Button Background="{x:Null}" BorderBrush="Yellow" BorderThickness="2,0,0,2" Margin="5,5,5,5" Padding="10,0,10,0" Command="{Binding ButtonRetryCommand}">Wiederholen</Button>
            <Button Background="{x:Null}" BorderBrush="Lime" BorderThickness="2,0,0,2" Margin="5,5,5,5" Padding="10,0,10,0" Command="{Binding ButtonOkCommand}">Ok</Button>
            <Button Background="{x:Null}" BorderBrush="Red" BorderThickness="2,0,0,2" Margin="5,5,5,5" Padding="10,0,10,0" Command="{Binding ButtonCancelCommand}">Abbrechen</Button>
        </StackPanel>
    </Grid>
</UserControl>
